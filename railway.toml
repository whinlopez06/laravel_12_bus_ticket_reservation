[build]
builder = "dockerfile"   # or "nixpacks" depending on your setup

[deploy]
postDeploy = "sleep 5 && php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache"
THIS-WILL-CAUSE-AN-ERROR
